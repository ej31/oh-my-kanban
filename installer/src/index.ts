/**
 * Phase-2 installer entrypoint stub.
 *
 * Intended future flow:
 * 1. Read provider metadata exported from the Python runtime package.
 * 2. Prompt for provider selection.
 * 3. Prompt for provider-specific config values.
 * 4. Install selected runtime packages or extras.
 * 5. Write canonical config.toml with provider namespaces.
 */

console.log("oh-my-kanban installer scaffold only");
