/**
 * Plane installer prompt stub.
 *
 * Future implementation should collect:
 * - base URL
 * - API key
 * - workspace slug
 * - optional project ID
 */

export const planeInstallerStub = {
  provider: "plane",
};
